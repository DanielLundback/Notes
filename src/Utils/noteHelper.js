export const notes1 = [
  {id: '1', title: 'My first note', body: 'Howdie hows it going'},
  {id: '2', title: 'another note', body: 'hello'},
]

export function getNote(id) {
  //notes.find = (element) => element.id === id
  //return
  //console.log('called Value', id)
}

function updateNote(id, title, body) {}

function deleteNote(id) {}
